# Προτεινόμενες Ασκήσεις & Παραδείγματα ανά Pattern (Week 3)

Αυτό το έγγραφο περιέχει 5 ολοκαίνουριες ιδέες πρακτικών προβλημάτων για το καθένα από τα 18 Patterns, σχεδιασμένες αποκλειστικά για τον Instructor. Κάτω από κάθε άσκηση υπάρχει ένα εννοιολογικό `hint` υλοποίησης.

## 1. The Accumulator (Result Variable)
1. **Υπολογισμός Παραγοντικού (Factorial):** Ένας πολλαπλασιαστικός accumulator που μαζεύει το γινόμενο των αριθμών από το 1 έως το N.
   ```
   Hint: Αρχικοποιείς result = 1. Στο loop, πολλαπλασιάζεις το τρέχον result με το i (result *= i).
   ```
2. **Μήκος χωρίς len():** Ένας μετρητής που αυξάνεται κατά 1 για κάθε χαρακτήρα ενός string, για να βρεις το μήκος του χειροκίνητα.
   ```
   Hint: Αρχικοποιείς count = 0. Στο range loop (που απλά διατρέχει το string), κάνεις count++ αγνοώντας την τιμή του χαρακτήρα.
   ```
3. **CSV Builder:** Έχεις ένα array από User IDs και θες να φτιάξεις ένα string της μορφής "id1,id2,id3".
   ```
   Hint: Αρχικοποιείς csv = "". Στο loop, προσθέτεις το ID και ένα κόμμα. Μετά το loop κόβεις το τελευταίο κόμμα (ή βάζεις το κόμμα μόνο αν δεν είσαι στο τελευταίο στοιχείο).
   ```
4. **Τελικό Υπόλοιπο Τράπεζας:** Δίνεται ένα array με συναλλαγές (θετικές, αρνητικές). Βρες το τελικό υπόλοιπο.
   ```
   Hint: Αρχικοποιείς balance = 0. Μέσα στο loop, απλά προσθέτεις την τρέχουσα συναλλαγή στο balance. Τα πρόσημα (+/-) αναλαμβάνουν τα υπόλοιπα.
   ```
5. **Αντιστροφή String (Reverse):** Ξεκινάς με άδειο string και σε κάθε επανάληψη προσθέτεις τον νέο χαρακτήρα πριν από το ήδη υπάρχον.
   ```
   Hint: reversed = "". Μέσα στο loop: reversed = string(char) + reversed. Προσοχή, ο νέος χαρακτήρας πάει ΜΠΡΟΣΤΑ.
   ```

## 2. Min/Max Tracking
1. **Ο Κοντύτερος Δρομέας:** Σε μια λίστα με χρόνους αθλητών (σε δευτερόλεπτα), βρες τον καλύτερο (μικρότερο) χρόνο.
   ```
   Hint: Αρχικοποιείς minTime = arr[0]. Αν το τρέχον time < minTime, το minTime παίρνει τη νέα τιμή.
   ```
2. **Το Πιο Ακριβό Καλάθι:** Δίνεται ένα 2D array (κάθε γραμμή είναι τιμές καλαθιού). Βρες ποιο καλάθι έχει το μεγαλύτερο συνολικό άθροισμα.
   ```
   Hint: maxCart = 0. Εξωτερικό loop ανά καλάθι, εσωτερικό loop αθροίζει. Αν το sum > maxCart, ενημερώνεις το max.
   ```
3. **Η Μεγαλύτερη Πτώση Θερμοκρασίας:** Βρες τη μεγαλύτερη διαφορά ανάμεσα σε δύο συνεχόμενες μέρες (abs(arr[i] - arr[i-1])).
   ```
   Hint: maxDrop = 0. Στο loop (από το index 1), υπολογίζεις τη διαφορά. Αν διαφορά > maxDrop, ανανεώνεις.
   ```
4. **Ο Πιο Κοντινός στο Μηδέν (Closest to Zero):** Δίνονται αριθμοί. Βρες ποιος είναι πιο κοντά στο 0 (ελάχιστη απόλυτη τιμή).
   ```
   Hint: closest = arr[0]. Συγκρίνεις την απόλυτη τιμή (abs). Αν abs(num) < abs(closest), το closest = num.
   ```
5. **Καλύτερο Σκορ σε Video Game:** Σε ένα stream σκορ, όποτε έρχεται νέο, αν ξεπερνάει το HighScore, το ενημερώνεις με μήνυμα.
   ```
   Hint: highScore = 0. Στο άπειρο/συνεχές loop, αν newScore > highScore, τύπωσε "Record!" και ανανέωσε τη μεταβλητή.
   ```

## 3. Boolean State Flag
1. **Αφαίρεση Πολλαπλών Κενών (Space Toggle):** Αν το flag inSpace είναι true, αγνοείς τα επόμενα κενά.
   ```
   Hint: Αρχικά inSpace = false. Αν βρεις ' ' και το flag είναι false, τυπώνεις ' ' και το κάνεις true. Αν βρεις γράμμα, το κάνεις false.
   ```
2. **Εντοπισμός Μπλοκ HTML:** Μόλις δεις '<', κατεβάζεις τον διακόπτη. Μόλις δεις '>', τον ανεβάζεις. Αποθηκεύεις μόνο το αληθινό κείμενο.
   ```
   Hint: isText = true. Όταν διαβάζεις '<' γίνεται false. Όταν διαβάζεις '>' γίνεται true. Κάνεις accumulate μόνο όταν isText == true.
   ```
3. **Ταλάντωση (Zig-Zag):** Διαβάζεις αριθμούς. Πρέπει να πηγαίνουν εναλλάξ "μικρός, μεγάλος". Το flag expectLarger αλλάζει συνεχώς.
   ```
   Hint: expectLarger = true. Ελέγχεις τη συνθήκη βάσει του flag. Στο τέλος της επανάληψης το κάνεις αντιστροφή: expectLarger = !expectLarger.
   ```
4. **Skip Block:** Αν βρεις "START", το record = true. Αν βρεις "STOP", το record = false. Τυπώνεις ό,τι υπάρχει ενδιάμεσα.
   ```
   Hint: Με string ανίχνευση. record = false. Μόλις δεις τη λέξη εκκίνησης, αλλάζει η κατάσταση. Το print τρέχει μόνο αν record == true.
   ```
5. **Βρέθηκε η Στόχευση:** Ραντάρ. Μόλις δεις τον εχθρό γίνεται true. Αν μετά το loop παρέμεινε false, βγάζεις μήνυμα "All Clear".
   ```
   Hint: found = false. Στο loop, αν δεις τον στόχο, found = true. Έξω από το loop, κάνεις if !found { print("Clear") }.
   ```

## 4. Early Exit (Guard Clauses)
1. **Έλεγχος Έγκυρου Κωδικού Χρώματος:** Ελέγχει HEX color. Early exits: μήκος != 7, δεν ξεκινάει από #, έχει άκυρο γράμμα.
   ```
   Hint: Αντί για if len == 7 { if startsWith... }, κάνεις if len != 7 return false, μετά if str[0] != '#' return false, κρατώντας τον κώδικα φλατ.
   ```
2. **Αναγνώριση Δίσεκτου Έτους:** Δεν διαιρείται με 4 -> false. Διαιρείται με 100 (αλλά όχι 400) -> false. Τέλος -> true.
   ```
   Hint: Τρία απλά διαδοχικά if (τα δύο πρώτα επιστρέφουν false, το τρίτο αν χρειάζεται). Στο τέλος return true.
   ```
3. **HTTP Request Validator:** Έλεγχος headers. Αν url == "" (exit), token == nil (exit), method != "POST" (exit).
   ```
   Hint: Σειρά από if που πετάνε errors αμέσως. Ό,τι επιβιώσει κάτω από αυτά τα if θεωρείται 100% valid request.
   ```
4. **Game Collision Check:** Αν ο παίκτης είναι εκτός οθόνης (X/Y) ή δεν υπάρχει εχθρός, return (γλιτώνεις πολύπλοκα μαθηματικά).
   ```
   Hint: 3 guard clauses ελέγχουν τα βασικά όρια. Το βαρύ math τρέχει μόνο αν ο κώδικας φτάσει κάτω-κάτω.
   ```
5. **Αρχικοποίηση Βάσης:** if db.Connected() return. if db.Config == nil return error.
   ```
   Hint: Προστατεύεις τη συνάρτηση από το να τρέξει διπλά ή με λανθασμένα δεδομένα, επιστρέφοντας αμέσως στην αρχή του function.
   ```

## 5. Loop Control (Break / Continue)
1. **Ανάγνωση μέχρι το EOF:** Διαβάζεις γραμμές από τον χρήστη σε άπειρο for. Μόλις δώσει "EXIT", κάνεις break.
   ```
   Hint: for { input = read(); if input == "EXIT" { break } }. Απλό και καθαρό.
   ```
2. **Φιλτράρισμα Αρχείων:** Ψάχνεις αρχεία σε ένα φάκελο. Αν η κατάληξη είναι .tmp, κάνεις continue.
   ```
   Hint: if hasExtension(".tmp") { continue }. Η συνέχεια του loop απλά αγνοείται για αυτό το αρχείο, χωρίς nested ifs.
   ```
3. **Όριο Προσπαθειών (Retry Limit):** Δοκιμάζεις να κατεβάσεις ένα αρχείο 5 φορές. Αν πετύχει, κάνεις break (ώστε να μην εξαντλήσεις τις 5).
   ```
   Hint: for i=0; i<5; i++ { err = download(); if err == nil { break } }.
   ```
4. **Έλεγχος Ύποπτων Λέξεων:** Διαβάζεις chat. Αν η λέξη υπάρχει στη λίστα απαγορευμένων, κάνεις continue για να μην τυπωθεί.
   ```
   Hint: Το continue προσπερνάει το print() που βρίσκεται στο τέλος του loop.
   ```
5. **Εντοπισμός Πρώτου Λάθους:** Ελέγχεις JSON. Στο πρώτο syntax error, βγάζεις μήνυμα και κάνεις break.
   ```
   Hint: Δεν θες να γεμίσεις την οθόνη με 100 λάθη. Μόλις βρεις το 1ο, σταματάς την επεξεργασία του αρχείου (break).
   ```

## 6. Universal Check
1. **Μόνο Αριθμοί; (IsNumeric):** Ελέγχεις αν ένα string περιέχει μόνο ψηφία '0'-'9'.
   ```
   Hint: Loop χαρακτήρα-χαρακτήρα. Αν ο χαρακτήρας δεν είναι ανάμεσα στο '0' και το '9', κάνεις κατευθείαν return false (falsification).
   ```
2. **Είναι Όλοι Ενήλικες;** Σε παρέα 5 ατόμων, αν έστω ένας έχει ηλικία < 18, όλοι τρώνε άκυρο.
   ```
   Hint: Loop στους φίλους. Αν friend.Age < 18 { return false }. Αν τελειώσει το loop, return true.
   ```
3. **Είναι Ταξινομημένο; (IsSorted):** Ελέγχεις αν ένα array είναι ήδη ταξινομημένο από το μικρότερο στο μεγαλύτερο.
   ```
   Hint: Συγκρίνεις κάθε arr[i] με το arr[i+1]. Αν arr[i] > arr[i+1], δεν είναι ταξινομημένο, άρα return false.
   ```
4. **Όλα Μηδέν;** Σε πίνακα χρεών, έλεγξε αν έχουν εξοφληθεί ΌΛΑ.
   ```
   Hint: Ψάχνεις αν υπάρχει έστω και ένα arr[i] > 0. Αν βρεις, σημαίνει ότι υπάρχει χρέος (return false). Αλλιώς return true.
   ```
5. **Ισχυρό Password:** Ελέγχει αν το password έχει 1 κεφαλαίο, 1 μικρό, 1 αριθμό.
   ```
   Hint: Αντί για universal, εδώ είναι existential (ψάχνουμε αν ΥΠΑΡΧΕΙ έστω ένα). Τρεις διακόπτες που γίνονται true όταν βρεθεί το αντίστοιχο. Στο τέλος return (hasUpper && hasLower && hasNum).
   ```

## 7. Previous Value (Deduplication)
1. **Μέτρηση Αλλαγών Φρουράς:** Array με IDs υπαλλήλων ανά ώρα. Μέτρησε πόσες "αλλαγές βάρδιας" έγιναν.
   ```
   Hint: prev = arr[0]. Αν το τρέχον arr[i] != prev, αυξάνεις έναν μετρητή και ενημερώνεις το prev.
   ```
2. **Αφαίρεση Διπλών (Sorted Array):** Έχεις ταξινομημένο array [1, 1, 2, 3, 3]. Τύπωσε μόνο τα μοναδικά.
   ```
   Hint: prev = -1. Αν το num != prev, τύπωσε το num και κάνε prev = num. Τα διπλότυπα αγνοούνται.
   ```
3. **Διορθωτής Πληκτρολογίου:** Περιόρισε τις συνεχόμενες επαναλήψεις του ίδιου χαρακτήρα σε το πολύ 2 (π.χ. "hhheee").
   ```
   Hint: Κρατάς τον προηγούμενο χαρακτήρα και έναν μετρητή εμφανίσεων. Αν char == prev, count++. Αν count > 2, δεν τυπώνεις. Αν αλλάξει το γράμμα, count = 1.
   ```
4. **Συμπίεση Διαστημάτων:** Αν 10 συνεχόμενα pings έδωσαν "SUCCESS", τύπωσε "SUCCESS x10".
   ```
   Hint: Όσο το string είναι ίδιο με το prev, μετράς. Μόλις το string αλλάξει, τυπώνεις το [prev, count] και μηδενίζεις.
   ```
5. **Ανίχνευση Κορυφής (Peak Finding):** Βρες αν η τρέχουσα τιμή μιας μετοχής είναι μεγαλύτερη από την προηγούμενη ΚΑΙ την επόμενη.
   ```
   Hint: loop από το 1 έως το n-1. Ελέγχεις if arr[i] > arr[i-1] && arr[i] > arr[i+1].
   ```

## 8. Two Pointers
1. **Έλεγχος Παλίνδρομου (Αριθμοί):** Ελέγχεις αν ένα array από integers διαβάζεται το ίδιο ανάποδα.
   ```
   Hint: start = 0, end = len-1. Σε ένα for start < end, ελέγχεις αν arr[start] != arr[end]. Μετά start++, end--.
   ```
2. **Διαχωρισμός (Partition) Θετικών-Αρνητικών:** Όλα τα αρνητικά αριστερά, όλα τα θετικά δεξιά.
   ```
   Hint: start και end. Ο start ψάχνει θετικό, ο end ψάχνει αρνητικό. Μόλις βρουν και οι δύο, κάνουν swap(arr[start], arr[end]).
   ```
3. **Συγχώνευση 2 Ταξινομημένων Arrays:** Ενώνεις δύο arrays σε ένα νέο (βάση του Merge Sort).
   ```
   Hint: i = 0 (στο 1ο array), j = 0 (στο 2ο array). Όποιο από τα δύο δείχνει σε μικρότερο νούμερο, μπαίνει στο νέο array και προχωράει ο δείκτης του.
   ```
4. **Dutch National Flag:** Ταξινόμηση 3 χρωμάτων (0, 1, 2) in-place.
   ```
   Hint: 3 δείκτες (low, mid, high). Τα 0 πάνε στην αρχή (swap με low), τα 2 στο τέλος (swap με high), τα 1 μένουν στη μέση.
   ```
5. **Find the Duplicate (Tortoise & Hare):** Βρες κύκλο σε array κάνοντας άλματα x1 και x2.
   ```
   Hint: Ο slow pointer προχωράει 1 βήμα, ο fast 2 βήματα. Αν συναντηθούν, υπάρχει κύκλος/διπλότυπο.
   ```

## 9. Sliding Window
1. **Moving Average (Fixed):** Κινητός μέσος όρος 7 ημερών για κρούσματα.
   ```
   Hint: Υπολογίζεις το άθροισμα των πρώτων 7. Μετά στο loop, προσθέτεις τη νέα μέρα (δεξιά) και αφαιρείς τη μέρα i-7 (αριστερά). Διαιρείς με το 7.
   ```
2. **Μέγιστος Αριθμός Φωνηέντων σε Υποσυμβολοσειρά (Fixed):** Ποιο παράθυρο μεγέθους k=5 έχει τα περισσότερα φωνήεντα.
   ```
   Hint: Στο παράθυρο μετράς فωνήεντα (με accumulator). Όταν κάνεις slide, αν το γράμμα που φεύγει ήταν φωνήεν, -1. Αν αυτό που μπαίνει είναι, +1.
   ```
3. **Μικρότερο Παράθυρο με Άθροισμα >= Target (Variable):** Το πιο ΜΙΚΡΟ (σε μήκος) κομμάτι που το άθροισμά του φτάνει το 15.
   ```
   Hint: Ο right προχωράει (+). Μόλις το sum >= 15, μπαίνεις σε εσωτερικό loop που προχωράει τον left (-) για να δεις πόσο μικρό μπορείς να το κάνεις κρατώντας το >= 15.
   ```
4. **Longest Substring of '1's after Flip (Variable):** Έχεις string από 0/1. Μπορείς να "γυρίσεις" το πολύ k μηδενικά σε άσους.
   ```
   Hint: Ο right προχωράει. Αν βρει 0, μειώνεις το k. Αν k < 0, προχωράς τον left. Αν ο left αφήσει πίσω του 0, αυξάνεις το k.
   ```
5. **Αναγνώριση Αναγραμματισμού (Fixed):** Βρες αν το παράθυρο μεγέθους M περιέχει ακριβώς τα ίδια γράμματα με τη λέξη στόχο.
   ```
   Hint: Διατηρείς δύο array/map συχνοτήτων. Κάθε φορά που το παράθυρο τσουλάει, ενημερώνεις το δικό σου map και το συγκρίνεις με του στόχου.
   ```

## 10. Map Fast Lookup
1. **Λεξικό Μετάφρασης:** Ένα map με αγγλικά προς ελληνικά. Για κάθε λέξη κειμένου, ψάξε ακαριαία στο map.
   ```
   Hint: Αν το map[word] υπάρχει, τύπωσε τη μετάφραση. Αλλιώς τύπωσε την αρχική λέξη. Κανένα απολύτως nested loop.
   ```
2. **IP Banlist:** Σε τεράστιο firewall, ρωτάς το map αν η IP είναι banned.
   ```
   Hint: if bannedIPs[request.IP] { drop() }. Η ταχύτητα είναι κρίσιμη, το map απαντάει αμέσως.
   ```
3. **Τοπικός Κατάλογος Προϊόντων (Cache):** Αντί να ζητάς την τιμή ενός Barcode από τη βάση (αργό), την ψάχνεις στο τοπικό map.
   ```
   Hint: if val, ok := cache[barcode]; ok { return val }. Αλλιώς, ρώτα τη βάση και γράψε το στο map για το μέλλον.
   ```
4. **Who is Missing?** Έχεις τα IDs όσων ήρθαν. Βρες ποιος από τους 100 εγγεγραμμένους απουσιάζει.
   ```
   Hint: Βάλε όσους ήρθαν σε ένα map. Μετά, τρέξε ένα for 1..100: αν !map[i], τύπωσε "Απουσιάζει".
   ```
5. **Intersection of Arrays:** Βρες τα κοινά στοιχεία 2 τεράστιων arrays.
   ```
   Hint: Βάλε όλα τα στοιχεία του Array1 σε ένα map. Κάνε loop στο Array2, και για κάθε στοιχείο ρώτα το map.
   ```

## 11. Target Lookup (Complement Map)
1. **Βρες την Έκπτωση:** Έχεις κουπόνι 50€. Βρες 2 προϊόντα που το άθροισμά τους να κάνει ΑΚΡΙΒΩΣ 50€.
   ```
   Hint: complement = 50 - price[i]. Αν υπάρχει στο map, βρήκες το ζευγάρι. Αν όχι, βάζεις το price[i] στο map.
   ```
2. **Three Sum (χρήση Two Sum):** Θέλεις να βρεις 3 αριθμούς που αθροίζουν στο 0.
   ```
   Hint: Για κάθε αριθμό arr[i], τρέχεις το Target Lookup pattern (Two Sum) για τους υπόλοιπους αριθμούς με στόχο το -arr[i].
   ```
3. **Subarray Sum Equals K (Advanced):** Υπολογίζεις το τρέχον (running) άθροισμα. Ψάχνεις στο map αν το runningSum - K έχει εμφανιστεί.
   ```
   Hint: Αντί να ψάχνεις ζευγάρια, το map αποθηκεύει *αθροίσματα*. Έτσι εντοπίζεις συνεχόμενα κομμάτια (subarrays).
   ```
4. **Ζευγάρι με Απόλυτη Διαφορά K:** Ψάχνεις αν υπάρχουν a, b ώστε a - b = K.
   ```
   Hint: Για κάθε αριθμό num, ελέγχεις στο map αν υπάρχει ήδη ο (num + K) ή ο (num - K).
   ```
5. **Συμπληρωματικά Χρώματα:** Λίστα με RGB χρώματα, ψάχνεις αν υπάρχει ακριβώς το ανεστραμμένο του χρώματος.
   ```
   Hint: Αποθηκεύεις τα RGB ως string (π.χ. "255,0,0"). Για κάθε χρώμα, υπολογίζεις το complement "0,255,255" και ρωτάς το map.
   ```

## 12. Frequency Counting
1. **Μέτρηση Ψήφων (Ballot Box):** Καταμέτρηση ψήφων από ονόματα, για να βρεις τον νικητή.
   ```
   Hint: Στο loop: votes[name]++. Στο τέλος βρίσκεις το κλειδί με τη μεγαλύτερη τιμή.
   ```
2. **Ιστόγραμμα Βαθμολογιών:** Πόσοι πήραν 10, πόσοι 9, πόσοι 8 κλπ.
   ```
   Hint: Map με keys (1-10). Διαβάζεις τους βαθμούς, histogram[grade]++. Στο τέλος τυπώνεις τον πίνακα.
   ```
3. **Duplicate IP Detection:** Ποιες IPs έκαναν πάνω από 100 requests.
   ```
   Hint: Μετράς τις εμφανίσεις κάθε IP. Μετά το loop, διατρέχεις το map και τυπώνεις όσες έχουν τιμή > 100.
   ```
4. **Βρες τον Μοναχικό Αριθμό:** Σε ένα array όλοι εμφανίζονται 2 φορές, ΕΚΤΟΣ από έναν.
   ```
   Hint: Φτιάχνεις το map συχνοτήτων. Μετά ψάχνεις ποιο κλειδί έχει τιμή == 1.
   ```
5. **Group Anagrams:** Ομαδοποίηση αναγραμματισμών (π.χ. "cat", "act").
   ```
   Hint: Ταξινομείς τα γράμματα κάθε λέξης (ώστε το cat και το act να γίνουν "act"). Χρησιμοποιείς το "act" ως κλειδί στο map!
   ```

## 13. Two-Pass Algorithm
1. **Κανονικοποίηση (Normalization):** Μετάτρεψε όλες τις τιμές μεταξύ 0 και 1.
   ```
   Hint: 1ο πέρασμα: Βρίσκεις το min και το max. 2ο πέρασμα: (val - min) / (max - min).
   ```
2. **Move Zeroes to End:** Στείλε όλα τα μηδενικά στο τέλος, διατηρώντας τη σειρά των υπολοίπων.
   ```
   Hint: 1ο πέρασμα: Αντέγραψε τους μη-μηδενικούς στην αρχή του array. 2ο πέρασμα: Από εκεί που έμεινες μέχρι το τέλος, βάλε 0.
   ```
3. **Φιλτράρισμα Αδύναμων Σημάτων:** Διέγραψε όσες μετρήσεις είναι κάτω από το 50% του Μέσου Όρου.
   ```
   Hint: 1ο πέρασμα: Accumulator για να βρεις τον Μέσο Όρο. 2ο πέρασμα: Κρατάς μόνο τα στοιχεία που είναι > (avg * 0.5).
   ```
4. **Find All Numbers Disappeared:** Σε array 1..N, βρες ποιοι αριθμοί λείπουν.
   ```
   Hint: 1ο πέρασμα: Χρησιμοποίησε την τιμή του στοιχείου ως Index και κάνε το αρνητικό (σημάδι). 2ο πέρασμα: Αν ένα index παρέμεινε θετικό, αυτός ο αριθμός έλειπε.
   ```
5. **Το Σπίτι με την Καλύτερη Θέα:** Ποια σπίτια βλέπουν θάλασσα (δεν μπλοκάρονται από τα δεξιά).
   ```
   Hint: 1ο πέρασμα (ανάποδα): Φτιάχνεις array με το max_height στα δεξιά κάθε σπιτιού. 2ο πέρασμα: Το συγκρίνεις με το ύψος του σπιτιού.
   ```

## 14. Custom Iteration
1. **Run-Length Decoding:** Διαβάζεις "3A2B" και θες να τυπώσεις "AAABB".
   ```
   Hint: i += 2 σε κάθε βήμα. Διαβάζεις τον αριθμό στο arr[i] και το γράμμα στο arr[i+1].
   ```
2. **Markdown Parser:** Αν δεις `**`, ξεκινάει Bold. Κάνεις skip το δεύτερο αστεράκι.
   ```
   Hint: Αν βρεις match, κάνεις i++ έξτρα, για να μην διαβάσει το δεύτερο αστεράκι στο επόμενο loop.
   ```
3. **Αφαίρεση Υποσυμβολοσειράς:** Αν βρεις "apple", την παραλείπεις τελείως.
   ```
   Hint: Αν βρεις "apple", i += 5. Αλλιώς, τυπώνεις το γράμμα και κάνεις κανονικά i++.
   ```
4. **Διάβασμα Matrix 2x2:** Διαβάζεις pixels σε τετράγωνα 2x2 αντί για 1x1.
   ```
   Hint: Το εξωτερικό loop κάνει y += 2, το εσωτερικό κάνει x += 2. Έτσι πηδάς τα ενδιάμεσα pixels.
   ```
5. **Jumping Game:** Κάθε κελί λέει πόσα βήματα ΜΠΟΡΕΙΣ να πηδήξεις.
   ```
   Hint: Δεν κάνεις καθόλου i++! Το επόμενο i είναι: i = i + arr[i]. Ελέγχεις απλά αν έφτασες στο τέλος.
   ```

## 15. Digit Extraction
1. **Harshad Number:** Διαιρείται τέλεια από το άθροισμα των ψηφίων του (π.χ. 18 / 9).
   ```
   Hint: Κάνεις extract τα ψηφία και τα αθροίζεις σε μια temp μεταβλητή. Στο τέλος, ελέγχεις originalNum % sum == 0.
   ```
2. **Happy Number:** Άθροισμα τετραγώνων ψηφίων μέχρι να φτάσεις στο 1.
   ```
   Hint: (num % 10)^2 + ((num/10) % 10)^2. Το βάζεις σε while loop μέχρι το άθροισμα να γίνει 1.
   ```
3. **Δυαδικό σε Δεκαδικό (Binary to Decimal):** Παίρνεις ένα-ένα τα ψηφία του δυαδικού με % 10.
   ```
   Hint: Πολλαπλασιάζεις το (num % 10) με το power_of_two, το προσθέτεις στο total, και διαιρείς το num με το 10. Μετά διπλασιάζεις το power_of_two.
   ```
4. **Palindromic Number:** Αντίστροφος αριθμός χωρίς string (π.χ. 123 -> 321).
   ```
   Hint: reversed = reversed * 10 + (num % 10). Χτίζεις τον ανάποδο αριθμό μαθηματικά, ένα ψηφίο τη φορά.
   ```
5. **Product of Digits:** Γινόμενο ψηφίων (αν έχει 0 γίνεται Early Exit).
   ```
   Hint: prod = 1. Στο loop, dig = num % 10. Αν dig == 0 return 0. Αλλιώς prod *= dig, num /= 10.
   ```

## 16. Modulo Cycles
1. **Κυκλικός Κατάλογος (Round Robin):** Μοίρασε 100 requests σε 5 servers κυκλικά.
   ```
   Hint: Ο server που θα αναλάβει το request N, είναι ο: serverIndex = N % 5.
   ```
2. **K-th Μετατόπιση Αλφαβήτου (Caesar Cipher):** Μετακίνησε το γράμμα 5 θέσεις.
   ```
   Hint: (charIndex + 5) % 26. Εξασφαλίζει ότι το 'Z' (25) θα κάνει κύκλο πίσω στο 'A' (0).
   ```
3. **Ticking Clock (60 Seconds):** Χρονόμετρο, αυξάνεις κατά 1 δευτερόλεπτο.
   ```
   Hint: sec = (sec + 1) % 60. Όταν η πράξη βγάλει 0 (δηλαδή έφτασε 60), ξέρεις ότι πρέπει να αυξήσεις τα λεπτά.
   ```
4. **Αλλαγή Κατεύθυνσης 2D (Compass):** [Βορράς(0), Ανατολή(1), Νότος(2), Δύση(3)]. Δεξιά στροφή.
   ```
   Hint: Αν είσαι Δύση (3), η δεξιά στροφή είναι +1. (3 + 1) % 4 = 0 (επιστρέφεις Βορρά).
   ```
5. **Εύρεση Μήνα:** Είμαστε στον Οκτώβριο (10). Ποιος μήνας θα είναι σε 15 μήνες;
   ```
   Hint: Επειδή οι μήνες είναι 1-12 (όχι 0-11), κάνουμε -1 πριν το modulo και +1 μετά: (10 + 15 - 1) % 12 + 1.
   ```

## 17. Stack (LIFO)
1. **Διαδρομή Φακέλου (Path Simplification):** Δίνεται path `/a/./b/../../c/`. Το `..` σημαίνει γύρνα πίσω.
   ```
   Hint: Αν το string είναι γράμμα, Push. Αν είναι `..`, κάνεις Pop (διαγράφεις το τελευταίο). Αν είναι `.`, δεν κάνεις τίποτα.
   ```
2. **HTML Linter:** Έλεγχος ότι τα tags κλείνουν σωστά.
   ```
   Hint: Στο <div> κάνεις Push. Στο </div> κάνεις Pop και ελέγχεις αν το tag που βγήκε ήταν όντως <div>. Στο τέλος το stack πρέπει να είναι άδειο.
   ```
3. **Ιστορικό Browser (Back Button):** Σελίδα -> Stack. Πάτημα Πίσω -> Pop.
   ```
   Hint: Push(URL). Στο Go Back, το Pop() σου δίνει το τελευταίο URL που επισκέφθηκες.
   ```
4. **Επόμενος Μεγαλύτερος Αριθμός:** Βρες τον πρώτο μεγαλύτερο αριθμό στα δεξιά κάθε στοιχείου.
   ```
   Hint: Το Stack κρατάει στοιχεία που "περιμένουν" να βρουν έναν μεγαλύτερο. Μόλις έρθει μεγάλος αριθμός, κάνει Pop όλους τους μικρότερους!
   ```
5. **Αξιολόγηση Πράξεων με Προτεραιότητα:** Υπολογιστής 3 4 + 2 * (RPN).
   ```
   Hint: Στους αριθμούς κάνεις Push. Στις πράξεις (π.χ. +), κάνεις Pop 2 αριθμούς, τους προσθέτεις, και κάνεις Push το αποτέλεσμα.
   ```

## 18. Greedy Algorithms
1. **Συμπίεση Αρχείων (Huffman Coding):** Επέλεξε πάντα να ενώσεις τα 2 γράμματα με τη μικρότερη συχνότητα.
   ```
   Hint: Άπληστα διαλέγεις τα 2 μικρότερα, τα αθροίζεις και τα ξαναβάζεις στη λίστα. Επαναλαμβάνεις.
   ```
2. **Ελαχιστοποίηση Καθυστερήσεων (Job Sequencing):** Εργασίες με deadlines και κέρδος.
   ```
   Hint: Ταξινομείς βάσει κέρδους. Άπληστα παίρνεις την πιο κερδοφόρα και την τοποθετείς ΟΣΟ πιο κοντά στο deadline της γίνεται.
   ```
3. **Μοίρασμα Cookies στα Παιδιά:** Παιδιά με απαιτήσεις μεγέθους, μπισκότα διαφόρων μεγεθών.
   ```
   Hint: Ταξινομείς παιδιά και μπισκότα. Δίνεις στο παιδί με τη μικρότερη απαίτηση, το μικρότερο μπισκότο που τον ικανοποιεί.
   ```
4. **Minimum Spanning Tree (Prim/Kruskal):** Ένωσε όλες τις πόλεις με το ελάχιστο κόστος δρόμων.
   ```
   Hint: Ταξινομείς τους δρόμους βάσει κόστους. Άπληστα επιλέγεις πάντα τον φθηνότερο, ΑΡΚΕΙ να μην δημιουργεί κύκλο με τους ήδη επιλεγμένους.
   ```
5. **Διάσωση με Βάρκες:** Βάρκες χωράνε 2 άτομα με όριο βάρους. Διέσωσε τους πάντες με τις λιγότερες βάρκες.
   ```
   Hint: Ταξινομείς τα άτομα. Προσπαθείς πάντα να βάλεις τον πιο ΒΑΡΥ μαζί με τον πιο ΕΛΑΦΡΥ. Αν δεν χωράνε, ο βαρύς φεύγει μόνος του.
   ```
