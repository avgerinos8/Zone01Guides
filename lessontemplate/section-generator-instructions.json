{
  "role": "Generate ONE section of a Greek slide deck as JS calls to paste into an existing script (Zone01 template). Output ONLY the calls, no prose. Self-contained — you do not need any other file.",
  "response_format": "Provide the output as an actual attached/downloadable file. Do not paste it as plain chat text. If the platform genuinely cannot attach files, wrap the ENTIRE output in five backticks on their own line before and after (````` ... `````) as one block.",
  "exact_syntax": "addContent(`...raw HTML...`, opts?); addQuiz([{q,options:[4],correct:idx,explain}, ...], opts?); addFillBlank([{code,blanks:[{id,answer}],explain,lang?}], opts?); addMatching([{term,def}, ...] | [set1, set2, ...], opts?) — backticks exactly as shown for addContent, real JS array/object literals for the rest.",
  "js_api": {
    "addContent(html, opts?)": "one slide; use only h1 h2 h3 p p.lede div.callout(.good) div.grid2/grid3>div.card pre>code table.glossary-table div.glossary-strip(pills wrap onto new lines when the row runs out of width, no scrollbar to worry about) div.glossary-columns div.sources-list>a — never plain unwrapped text",
    "addQuiz([{q,options:[4],correct:idx,explain}], opts?)": "1+ MC questions on one slide. q/options[] default to plain text; set opts.allowHTML:true on this call to allow <br> or a <pre><code class=\"lang-...\">...</code></pre> inside q or an option (see opts.allowHTML)",
    "addFillBlank([{code,blanks:[{id,answer}],explain,lang?}], opts?)": "mark blanks as __id__ in code; Check/Reveal-hint/wrong-answer UI is automatic, don't build it yourself. Optional per-item lang (e.g. \"go\") auto-colors the static code text with the same tokens as a lang-go code block — omit for plain uncolored code",
    "addMatching([{term,def}] | [[...],[...]], opts?)": "matching pairs; pass an array of arrays for 2-3 independent sets on one slide. term/def default to plain text; set opts.allowHTML:true on this call to allow <br> or a <pre><code class=\"lang-...\">...</code></pre> inside a term or def (see opts.allowHTML)"
  },
  "opts": {
    "label": "override header text",
    "note": "Greek instructional note under header",
    "bg": "background image path, works on all 4 functions, default none",
    "overlayAlpha": "0..1, default 0.5",
    "backgroundSize": "cover|contain|auto(default)|\"100% 100%\"(stretch)|\"100% auto\"(fit-width)",
    "backgroundRepeat": "no-repeat(default)|repeat|repeat-x|repeat-y",
    "backgroundPosition": "CSS position value, default \"top left\", e.g. \"center center\"",
    "allowHTML": "boolean, default false, addQuiz/addMatching ONLY — when true, q/options[]/term/def are parsed as real HTML instead of plain text; use only when a <br> or <pre><code> is genuinely needed there, and still escape < > & for anything not meant to be an actual tag"
  },
  "spoiler_minigame": "Optional drag-to-reveal block, embeddable inside addContent(): <div class=\"spoiler\"><div class=\"spoiler-lock\"><span>Drag to reveal →</span></div><pre><code>...</code></pre></div> — unlocks only by dragging, not clicking.",
  "code_blocks": "Colored code in addContent(): hand-wrap words in <span class=\"tok-kw|tok-type|tok-str|tok-com|tok-num|tok-func\"> yourself, no auto-highlighter there. Escape < > & as &lt; &gt; &amp; inside code text (it sits inside HTML). In addFillBlank(), prefer item.lang (e.g. \"go\") over hand-wrapping — it colors the static code text automatically; fall back to manual tok-* spans only if item.lang's language list doesn't cover what's needed.",
  "scoring_note": "Quiz/fillblank/matching are scored automatically by the engine — don't add score text yourself, it's handled elsewhere.",
  "rules": [
    "prose Greek, code comments English",
    "quiz options same length/style, correct slot varies, distractors plausible not silly, one ~5-words-longer distractor somewhere",
    "code blocks self-contained and fully commented",
    "do not solve the actual exercise — theory/tools only",
    "do not include title/sources/init slides — only this section's content"
  ],
  "section_topic": "<< FILL IN >>",
  "eyebrow_convention": "Every addContent() slide opens with <div class=\"eyebrow\">Ενότητα N / TOTAL — <Section Title></div> immediately before its <h1> — same eyebrow text repeated on every slide of that section, don't renumber mid-section. Required: a later tool (extract_toc) reads this exact pattern to build the sidebar TOC; a missing/malformed eyebrow silently drops that slide from it.",
  "slugs_and_toc": "Do not add id=\"@...\" or any #toc-list content yourself — separate, later step (extract_toc, a Go tool) plus manual curation, run after this content exists. Only thing that matters here is eyebrow_convention above, since that tool depends on it."
}