# Προτεινόμενες Ασκήσεις & Παραδείγματα ανά Pattern (Week 3)

Αυτό το έγγραφο περιέχει 5 ολοκαίνουριες ιδέες πρακτικών προβλημάτων για το καθένα από τα 18 Patterns. Οι εκφωνήσεις είναι πλέον πιο αναλυτικές, δίνοντας ξεκάθαρο context για το τι πρέπει να πετύχει ο μαθητής. Κάτω από κάθε άσκηση υπάρχει ένα εννοιολογικό `hint` υλοποίησης.

## 1. The Accumulator (Result Variable)
1. **Υπολογισμός Παραγοντικού (Factorial):** Γράψτε ένα πρόγραμμα που δέχεται έναν θετικό ακέραιο `N` και υπολογίζει το παραγοντικό του (`N!`). Για παράδειγμα, αν δοθεί το `N=5`, το αποτέλεσμα πρέπει να είναι `1 * 2 * 3 * 4 * 5 = 120`. Χρησιμοποιήστε μια μεταβλητή για να χτίσετε το αποτέλεσμα.
   ```
   Hint: Αρχικοποιείς result = 1. Στο loop, πολλαπλασιάζεις το τρέχον result με το i (result *= i).
   ```
2. **Μήκος χωρίς την len():** Στη Go (αλλά και σε πολλές άλλες γλώσσες) υπάρχει η συνάρτηση `len()`. Γράψτε μια δική σας συνάρτηση που παίρνει ένα string, διατρέχει όλους τους χαρακτήρες του, και επιστρέφει πόσοι είναι συνολικά, προσομοιώνοντας τη συμπεριφορά της `len()`.
   ```
   Hint: Αρχικοποιείς count = 0. Στο range loop (που απλά διατρέχει το string), κάνεις count++ αγνοώντας την τιμή του χαρακτήρα.
   ```
3. **CSV Builder:** Σας δίνεται ένα array από User IDs (π.χ. `[101, 102, 105]`). Ο στόχος σας είναι να φτιάξετε ένα ενιαίο string όπου όλα τα IDs θα είναι ενωμένα με κόμμα, δηλαδή να επιστρέψετε `"101,102,105"`.
   ```
   Hint: Αρχικοποιείς csv = "". Στο loop, προσθέτεις το ID και ένα κόμμα. Μετά το loop κόβεις το τελευταίο κόμμα (ή βάζεις το κόμμα μόνο αν δεν είσαι στο τελευταίο στοιχείο).
   ```
4. **Τελικό Υπόλοιπο Τράπεζας:** Μια τράπεζα σας δίνει ένα array με τις ημερήσιες συναλλαγές ενός πελάτη. Οι θετικοί αριθμοί είναι καταθέσεις και οι αρνητικοί είναι αναλήψεις (π.χ. `[100, -20, -10, 50]`). Υπολογίστε ποιο θα είναι το τελικό υπόλοιπο του λογαριασμού αν ξεκινήσει από το μηδέν.
   ```
   Hint: Αρχικοποιείς balance = 0. Μέσα στο loop, απλά προσθέτεις την τρέχουσα συναλλαγή στο balance. Τα πρόσημα (+/-) αναλαμβάνουν τα υπόλοιπα.
   ```
5. **Αντιστροφή String (Reverse):** Σας δίνεται η λέξη `"hello"`. Σκοπός είναι να δημιουργήσετε μια νέα μεταβλητή που να περιέχει την ίδια λέξη αλλά γραμμένη ανάποδα (`"olleh"`), διαβάζοντας την αρχική λέξη κανονικά, γράμμα-γράμμα από την αρχή προς το τέλος.
   ```
   Hint: reversed = "". Μέσα στο loop: reversed = string(char) + reversed. Προσοχή, ο νέος χαρακτήρας πάει ΜΠΡΟΣΤΑ.
   ```

## 2. Min/Max Tracking
1. **Ο Κοντύτερος Δρομέας:** Έχετε καταγράψει τους χρόνους 5 αθλητών στα 100 μέτρα, σε δευτερόλεπτα (π.χ. `[10.5, 9.8, 11.2, 10.1]`). Βρείτε ποιος ήταν ο καλύτερος χρόνος, δηλαδή ποιος αριθμός είναι ο μικρότερος στη λίστα.
   ```
   Hint: Αρχικοποιείς minTime = arr[0]. Αν το τρέχον time < minTime, το minTime παίρνει τη νέα τιμή.
   ```
2. **Το Πιο Ακριβό Καλάθι:** Ένα 2D array (πίνακας) περιέχει τα καλάθια αγορών διαφόρων πελατών. Κάθε γραμμή είναι οι τιμές των προϊόντων που πήρε ένας πελάτης (π.χ. `[ [5, 10], [20, 2], [50] ]`). Βρείτε ποιο καλάθι έχει το μεγαλύτερο συνολικό κόστος.
   ```
   Hint: maxCart = 0. Εξωτερικό loop ανά καλάθι, εσωτερικό loop αθροίζει. Αν το sum > maxCart, ενημερώνεις το max.
   ```
3. **Η Μεγαλύτερη Πτώση Θερμοκρασίας:** Σας δίνεται ένα array με τις θερμοκρασίες ενός μήνα. Βρείτε τη μέρα που καταγράφηκε η μεγαλύτερη *πτώση* ή *άνοδος* (διαφορά) σε σχέση με την ακριβώς προηγούμενη ημέρα. Θα χρειαστείτε την απόλυτη τιμή της διαφοράς.
   ```
   Hint: maxDrop = 0. Στο loop (από το index 1), υπολογίζεις τη διαφορά (abs(arr[i] - arr[i-1])). Αν > maxDrop, ανανεώνεις.
   ```
4. **Ο Πιο Κοντινός στο Μηδέν (Closest to Zero):** Δίνεται ένα array με θετικούς και αρνητικούς αριθμούς. Βρείτε ποιος αριθμός βρίσκεται πιο κοντά στο μηδέν (π.χ. ανάμεσα στο 5 και το -2, το -2 είναι πιο κοντά). Σε περίπτωση ισοπαλίας, προτιμήστε τον θετικό.
   ```
   Hint: closest = arr[0]. Συγκρίνεις την απόλυτη τιμή (abs). Αν abs(num) < abs(closest), το closest = num.
   ```
5. **Καλύτερο Σκορ σε Video Game:** Διαβάζετε ένα συνεχές (infinite) stream από σκορς παικτών. Το πρόγραμμα πρέπει να κρατάει το μεγαλύτερο σκορ όλων των εποχών (HighScore). Όποτε ένας νέος παίκτης ξεπερνάει αυτό το σκορ, να τυπώνεται στην οθόνη "New Record!".
   ```
   Hint: highScore = 0. Στο άπειρο/συνεχές loop, αν newScore > highScore, τύπωσε "Record!" και ανανέωσε τη μεταβλητή.
   ```

## 3. Boolean State Flag
1. **Αφαίρεση Πολλαπλών Κενών (Space Toggle):** Έχετε ένα κείμενο που μπορεί να έχει πολλά συνεχόμενα κενά (π.χ. `"Hello     world"`). Θέλετε να το τυπώσετε κρατώντας μόνο ένα κενό ανάμεσα στις λέξεις. Χρησιμοποιήστε έναν διακόπτη για να θυμάστε αν "μόλις τυπώσατε κενό".
   ```
   Hint: Αρχικά inSpace = false. Αν βρεις ' ' και το flag είναι false, τυπώνεις ' ' και το κάνεις true. Αν βρεις γράμμα, το κάνεις false.
   ```
2. **Εντοπισμός Μπλοκ HTML:** Διαβάζετε έναν κώδικα HTML (π.χ. `<p>Hello</p>`). Θέλετε να τυπώσετε ΜΟΝΟ το κείμενο που βρίσκεται έξω από τα tags, δηλαδή αγνοώντας ό,τι βρίσκεται ανάμεσα στα `<` και `>`. Ένας διακόπτης θα σας λέει πότε είστε μέσα σε tag.
   ```
   Hint: isText = true. Όταν διαβάζεις '<' γίνεται false. Όταν διαβάζεις '>' γίνεται true. Κάνεις accumulate μόνο όταν isText == true.
   ```
3. **Ταλάντωση (Zig-Zag):** Θέλετε να ελέγξετε αν μια σειρά αριθμών κάνει "ζιγκ-ζαγκ", δηλαδή ο πρώτος είναι μικρότερος από τον δεύτερο, ο δεύτερος μεγαλύτερος από τον τρίτο, ο τρίτος μικρότερος κ.ο.κ. Ένας διακόπτης πρέπει να εναλλάσσεται σε κάθε βήμα.
   ```
   Hint: expectLarger = true. Ελέγχεις τη συνθήκη βάσει του flag. Στο τέλος της επανάληψης το κάνεις αντιστροφή: expectLarger = !expectLarger.
   ```
4. **Skip Block:** Διαβάζετε ένα μεγάλο log αρχείο. Σας ενδιαφέρει να αποθηκεύσετε τα δεδομένα ΜΟΝΟ από τη στιγμή που θα συναντήσετε τη λέξη "START", μέχρι τη στιγμή που θα βρείτε τη λέξη "STOP". Τα υπόλοιπα πρέπει να αγνοηθούν.
   ```
   Hint: Με string ανίχνευση. record = false. Μόλις δεις τη λέξη εκκίνησης, αλλάζει η κατάσταση. Το print τρέχει μόνο αν record == true.
   ```
5. **Βρέθηκε η Στόχευση:** Διαβάζετε τις μετρήσεις ενός ραντάρ σε ένα loop. Αν εντοπίσετε το νούμερο του εχθρού (π.χ. 99), σηκώνετε τον συναγερμό. Αν το loop ολοκληρωθεί και το ραντάρ δεν έπιασε τίποτα, τυπώστε το μήνυμα "All Clear".
   ```
   Hint: found = false. Στο loop, αν δεις τον στόχο, found = true. Έξω από το loop, κάνεις if !found { print("Clear") }.
   ```

## 4. Early Exit (Guard Clauses)
1. **Έλεγχος Έγκυρου Κωδικού Χρώματος:** Φτιάξτε μια συνάρτηση που επιστρέφει `true` αν ένα string είναι έγκυρο χρώμα HEX. Πρέπει να έχει ακριβώς 7 χαρακτήρες, ο πρώτος να είναι το `#`, και να μην έχει γράμματα πάνω από το F. Κάντε exit αμέσως μόλις βρείτε ένα λάθος, αντί να βάλετε τα πάντα μέσα σε if.
   ```
   Hint: Αντί για if len == 7 { if startsWith... }, κάνεις if len != 7 return false, μετά if str[0] != '#' return false, κρατώντας τον κώδικα φλατ.
   ```
2. **Αναγνώριση Δίσεκτου Έτους:** Ένα έτος είναι δίσεκτο αν διαιρείται με το 4. Εξαίρεση: αν διαιρείται με το 100 ΔΕΝ είναι, εκτός κι αν διαιρείται με το 400 που τελικά είναι. Σπάστε αυτή τη λογική σε μικρά, ξεχωριστά early exits.
   ```
   Hint: Τρία απλά διαδοχικά if (τα δύο πρώτα επιστρέφουν false, το τρίτο αν χρειάζεται). Στο τέλος return true.
   ```
3. **HTTP Request Validator:** Μια συνάρτηση παίρνει τα headers μιας αίτησης. Αν το url είναι άδειο, αν το token λείπει, ή αν η μέθοδος δεν είναι POST, πρέπει να διακόψει την εκτέλεση αμέσως πετώντας error. Διαφορετικά προχωράει στην αποθήκευση.
   ```
   Hint: Σειρά από if που πετάνε errors αμέσως. Ό,τι επιβιώσει κάτω από αυτά τα if θεωρείται 100% valid request.
   ```
4. **Game Collision Check:** Σε ένα παιχνίδι, η συνάρτηση `checkCollision()` κάνει βαρείς μαθηματικούς υπολογισμούς. Πριν τους κάνει, ελέγξτε αν ο παίκτης είναι εκτός οθόνης (ώστε να τερματίσετε τη συνάρτηση αμέσως) για να γλιτώσετε CPU.
   ```
   Hint: 3 guard clauses ελέγχουν τα βασικά όρια. Το βαρύ math τρέχει μόνο αν ο κώδικας φτάσει κάτω-κάτω.
   ```
5. **Αρχικοποίηση Βάσης Δεδομένων:** Φτιάχνετε τη συνάρτηση `connectDB()`. Αν το σύστημα λέει ότι είστε ήδη συνδεδεμένοι, σταματήστε αμέσως. Αν λείπει το αρχείο ρυθμίσεων, σταματήστε αμέσως βγάζοντας error. Αν όλα πάνε καλά, τρέξτε τη βαριά σύνδεση στο τέλος της συνάρτησης.
   ```
   Hint: Προστατεύεις τη συνάρτηση από το να τρέξει διπλά ή με λανθασμένα δεδομένα, επιστρέφοντας αμέσως στην αρχή του function.
   ```

## 5. Loop Control (Break / Continue)
1. **Ανάγνωση μέχρι το EOF:** Φτιάξτε ένα άπειρο loop `for {}` που ζητάει συνεχώς από τον χρήστη να πληκτρολογήσει μια εντολή. Αν ο χρήστης πληκτρολογήσει τη λέξη "EXIT", το πρόγραμμα πρέπει να τερματίσει το loop και να κλείσει.
   ```
   Hint: for { input = read(); if input == "EXIT" { break } }. Απλό και καθαρό.
   ```
2. **Φιλτράρισμα Αρχείων:** Διαβάζετε τα ονόματα 100 αρχείων. Θέλετε να τυπώσετε το όνομα του καθενός. Όμως, αν δείτε ότι κάποιο αρχείο καταλήγει σε `.tmp`, θέλετε να το αγνοήσετε (skip) και να περάσετε κατευθείαν στο επόμενο, χωρίς να διακοπεί το loop.
   ```
   Hint: if hasExtension(".tmp") { continue }. Η συνέχεια του loop απλά αγνοείται για αυτό το αρχείο, χωρίς nested ifs.
   ```
3. **Όριο Προσπαθειών (Retry Limit):** Το δίκτυο έχει πρόβλημα και το download αποτυγχάνει. Θέλετε να γράψετε ένα loop που δοκιμάζει το πολύ 5 φορές. Αν πετύχει, π.χ. στην 2η προσπάθεια, πρέπει να σταματήσει αμέσως να προσπαθεί.
   ```
   Hint: for i=0; i<5; i++ { err = download(); if err == nil { break } }.
   ```
4. **Έλεγχος Ύποπτων Λέξεων:** Ένα bot διαβάζει τα μηνύματα σε ένα chat. Αν το μήνυμα περιέχει βρισιά, πρέπει να αγνοηθεί και να μην αποθηκευτεί στη βάση, αλλά το bot πρέπει να συνεχίσει να διαβάζει τα επόμενα μηνύματα.
   ```
   Hint: Το continue προσπερνάει το print() ή τη λειτουργία αποθήκευσης που βρίσκεται στο τέλος του loop.
   ```
5. **Εντοπισμός Πρώτου Λάθους:** Διαβάζετε ένα μεγάλο αρχείο JSON. Στο πρώτο συντακτικό λάθος που θα βρείτε, δεν υπάρχει λόγος να συνεχίσετε, καθώς το αρχείο είναι κατεστραμμένο. Σταματήστε το loop εκείνη τη στιγμή βγάζοντας error.
   ```
   Hint: Δεν θες να γεμίσεις την οθόνη με 100 λάθη. Μόλις βρεις το 1ο, σταματάς την επεξεργασία του αρχείου (break).
   ```

## 6. Universal Check
1. **Μόνο Αριθμοί; (IsNumeric):** Πώς μπορείτε να είστε 100% σίγουροι ότι το string `"12345"` δεν κρύβει κάπου ένα γράμμα; Ελέγξτε κάθε χαρακτήρα: αν έστω ένας δεν ανήκει στους αριθμούς ('0' έως '9'), τότε ολόκληρο το string απορρίπτεται.
   ```
   Hint: Loop χαρακτήρα-χαρακτήρα. Αν ο χαρακτήρας δεν είναι ανάμεσα στο '0' και το '9', κάνεις κατευθείαν return false (falsification).
   ```
2. **Είναι Όλοι Ενήλικες;** Έχετε τα δεδομένα μιας παρέας 5 ατόμων που θέλει να μπει σε club. Αν έστω και ΕΝΑΣ από την παρέα είναι κάτω από 18 χρονών, ολόκληρη η παρέα δεν μπορεί να μπει (η συνάρτηση επιστρέφει false).
   ```
   Hint: Loop στους φίλους. Αν friend.Age < 18 { return false }. Αν τελειώσει το loop (κανένας δεν ήταν κάτω από 18), return true.
   ```
3. **Είναι Ταξινομημένο; (IsSorted):** Σας δίνουν ένα array και σας λένε ότι είναι ήδη ταξινομημένο από το μικρότερο στο μεγαλύτερο. Πώς θα το ελέγξετε για να το επιβεβαιώσετε; (Έστω και μια παραφωνία, καταστρέφει τον κανόνα).
   ```
   Hint: Συγκρίνεις κάθε arr[i] με το arr[i+1]. Αν arr[i] > arr[i+1], δεν είναι ταξινομημένο, άρα return false.
   ```
4. **Όλα Μηδέν;** Μια βάση δεδομένων έχει τα χρέη ενός πελάτη προς διάφορες τράπεζες `[0, 0, 0, 50, 0]`. Θέλουμε να ελέγξουμε αν τα έχει εξοφλήσει ΟΛΑ, προκειμένου να του δώσουμε νέο δάνειο.
   ```
   Hint: Ψάχνεις αν υπάρχει έστω και ένα arr[i] > 0. Αν βρεις, σημαίνει ότι υπάρχει χρέος (return false). Αλλιώς return true.
   ```
5. **Ισχυρό Password:** Ένα password θεωρείται ισχυρό μόνο αν ικανοποιεί 3 διαφορετικές συνθήκες: Πρέπει να περιέχει (τουλάχιστον ένα) κεφαλαίο γράμμα, (τουλάχιστον ένα) μικρό, και (τουλάχιστον έναν) αριθμό.
   ```
   Hint: Αντί για universal, εδώ είναι existential (ψάχνουμε αν ΥΠΑΡΧΕΙ έστω ένα). Τρεις διακόπτες (hasUpper, hasLower, hasNum). Στο τέλος return και τα 3.
   ```

## 7. Previous Value (Deduplication)
1. **Μέτρηση Αλλαγών Φρουράς:** Σας δίνεται ένα array που δείχνει το ID του υπαλλήλου σε κάθε ώρα της βάρδιας (π.χ. `[1, 1, 1, 2, 2, 1]`). Μετρήστε πόσες φορές η βάρδια άλλαξε χέρια (δηλαδή ο τρέχων υπάλληλος δεν είναι ο ίδιος με της προηγούμενης ώρας).
   ```
   Hint: prev = arr[0]. Αν το τρέχον arr[i] != prev, αυξάνεις έναν μετρητή και ενημερώνεις το prev = arr[i].
   ```
2. **Αφαίρεση Διπλών (Sorted Array):** Σας δίνουν ένα ταξινομημένο array, π.χ. `[1, 1, 2, 3, 3]`. Τυπώστε τα στοιχεία στην οθόνη, αλλά μην τυπώσετε ποτέ το ίδιο νούμερο δύο φορές. Τα διπλότυπα πρέπει να αγνοηθούν.
   ```
   Hint: prev = -1 (ή κάτι άκυρο). Αν το num != prev, τύπωσε το num και κάνε prev = num. Τα διπλότυπα θα κοπούν.
   ```
3. **Διορθωτής Πληκτρολογίου:** Κάποιος κράτησε πατημένο το πλήκτρο και έγραψε `"hhheeeellooo"`. Γράψτε ένα πρόγραμμα που διαβάζει τα γράμματα, αλλά δεν αφήνει ΠΟΤΕ το ίδιο γράμμα να επαναληφθεί πάνω από 2 συνεχόμενες φορές.
   ```
   Hint: Κρατάς τον προηγούμενο χαρακτήρα και έναν μετρητή εμφανίσεων. Αν char == prev, count++. Αν count > 2, δεν τυπώνεις. Αν αλλάξει το γράμμα, count = 1.
   ```
4. **Συμπίεση Διαστημάτων:** Ένα σύστημα κάνει Ping κάθε δευτερόλεπτο και βγάζει "SUCCESS" ή "FAIL". Αν έχουμε 10 συνεχόμενα "SUCCESS", μην τα τυπώσετε 10 φορές. Τυπώστε μια φορά το `"SUCCESS x10"`.
   ```
   Hint: Όσο το string είναι ίδιο με το prev, απλά μετράς. Μόλις το string αλλάξει, τυπώνεις το [prev, count] και μηδενίζεις.
   ```
5. **Ανίχνευση Κορυφής (Peak Finding):** Βρείτε αν σε ένα γράφημα τιμών μετοχής υπάρχει μια "κορυφή" — δηλαδή μια μέρα που η τιμή ήταν μεγαλύτερη από τη χθεσινή (άρα είχε άνοδο), αλλά και μεγαλύτερη από την αυριανή (άρα μετά έπεσε).
   ```
   Hint: loop από το 1 έως το n-2. Ελέγχεις if arr[i] > arr[i-1] && arr[i] > arr[i+1].
   ```

## 8. Two Pointers
1. **Έλεγχος Παλίνδρομου (Αριθμοί):** Αν σας δοθεί ένα array με αριθμούς `[1, 2, 3, 2, 1]`, πώς θα ελέγξετε αν διαβάζεται το ίδιο και από την ανάποδη; Αντί να φτιάξετε νέο, ανάποδο array, ελέγξτε τις άκρες του!
   ```
   Hint: start = 0, end = len-1. Σε ένα for start < end, ελέγχεις αν arr[start] != arr[end]. Μετά start++, end--.
   ```
2. **Διαχωρισμός (Partition) Θετικών-Αρνητικών:** Έχετε ένα array γεμάτο θετικούς και αρνητικούς. Χρησιμοποιώντας δύο δείκτες στις άκρες, σπρώξτε όλους τους αρνητικούς στην αρχή του array και όλους τους θετικούς στο τέλος, κάνοντας in-place swaps.
   ```
   Hint: start και end. Ο start ψάχνει θετικό, ο end ψάχνει αρνητικό. Μόλις βρουν και οι δύο, κάνουν swap(arr[start], arr[end]).
   ```
3. **Συγχώνευση 2 Ταξινομημένων Arrays:** Σας δίνονται δύο ήδη ταξινομημένα arrays (`A` και `B`). Ενώστε τα σε ένα νέο τρίτο array που θα είναι επίσης ταξινομημένο, επιλέγοντας κάθε φορά το μικρότερο από τα δύο στοιχεία που "κοιτάνε" οι δείκτες.
   ```
   Hint: i = 0 (στο 1ο array), j = 0 (στο 2ο array). Όποιο από τα δύο δείχνει σε μικρότερο νούμερο, μπαίνει στο νέο array και προχωράει ο δείκτης του.
   ```
4. **Dutch National Flag:** Ένα array έχει μόνο τρεις αριθμούς: 0, 1, και 2 (σαν τα χρώματα μιας σημαίας). Ταξινομήστε το array (όλα τα 0 στην αρχή, τα 1 στη μέση, τα 2 στο τέλος) κάνοντας μόνο ένα πέρασμα, χρησιμοποιώντας 3 pointers.
   ```
   Hint: 3 δείκτες (low, mid, high). Τα 0 πάνε στην αρχή (swap με low), τα 2 στο τέλος (swap με high), τα 1 μένουν στη μέση.
   ```
5. **Find the Duplicate (Tortoise & Hare):** Σε ένα array που οδηγεί σαν λαβύρινθος (η κάθε τιμή σε πάει στο επόμενο index), βρείτε αν υπάρχει κύκλος, βάζοντας δύο δείκτες να τρέχουν — ο ένας 1 βήμα τη φορά (χελώνα), ο άλλος 2 βήματα (λαγός).
   ```
   Hint: Ο slow pointer προχωράει 1 βήμα, ο fast 2 βήματα. Αν κάποια στιγμή βρεθούν στο ίδιο index (συμπέσουν), υπάρχει κύκλος.
   ```

## 9. Sliding Window
1. **Moving Average (Fixed):** Οι τιμές του ρεύματος μιας πόλης αλλάζουν καθημερινά. Υπολογίστε τον μέσο όρο τιμής ανά "εβδομάδα" (κινητός μέσος όρος 7 ημερών) για όλο τον μήνα, χωρίς να υπολογίζετε ξανά και ξανά το άθροισμα 7 ημερών!
   ```
   Hint: Βρίσκεις το άθροισμα των πρώτων 7. Στη συνέχεια, προσθέτεις τη νέα μέρα (δεξιά) και αφαιρείς την παλιά μέρα (αριστερά).
   ```
2. **Μέγιστος Αριθμός Φωνηέντων (Fixed):** Δίνεται ένα μεγάλο κείμενο και ο αριθμός `K=5`. Βρείτε ποια "φέτα" από 5 συνεχόμενα γράμματα περιέχει τα περισσότερα φωνήεντα, τσουλώντας το παράθυρο K γραμμάτων κατά μήκος της λέξης.
   ```
   Hint: Μετράς φωνήεντα στο πρώτο παράθυρο. Όταν τσουλήσεις, αν το γράμμα που βγήκε ήταν φωνήεν κάνεις -1, αν το νέο που μπήκε ήταν κάνεις +1.
   ```
3. **Μικρότερο Παράθυρο (Variable):** Δίνεται ένα array και ένας στόχος, π.χ. `S=15`. Βρείτε το ΜΙΚΡΟΤΕΡΟ ΣΕ ΜΗΚΟΣ συνεχόμενο κομμάτι του array (subarray) που το άθροισμά του ξεπερνάει ή ισούται με το 15.
   ```
   Hint: Ο right pointer μεγαλώνει το παράθυρο. Μόλις το άθροισμα >= 15, ο left pointer αρχίζει να μικραίνει το παράθυρο (αφαιρώντας) για να δούμε πόσο μικρό μπορεί να γίνει.
   ```
4. **Longest Substring of '1's after Flip (Variable):** Σε ένα array με 0 και 1, σας επιτρέπεται να αλλάξετε το πολύ `K` μηδενικά σε άσους. Βρείτε ποια είναι η μεγαλύτερη συνεχόμενη σειρά από '1' που μπορείτε να δημιουργήσετε.
   ```
   Hint: Ο δεξιός δείκτης καταπίνει τα στοιχεία. Αν βρει 0, μειώνει το K. Αν το K εξαντληθεί, ο αριστερός δείκτης μαζεύει το παράθυρο μέχρι να βγάλει έξω ένα 0.
   ```
5. **Αναγνώριση Αναγραμματισμού (Fixed):** Θέλετε να βρείτε αν μέσα σε ένα τεράστιο string (π.χ. `cbaebabacd`) κρύβεται κάποιος αναγραμματισμός της λέξης `"abc"`. Τσουλήστε ένα παράθυρο μήκους 3 και ελέγχετε αν έχει τα ίδια γράμματα.
   ```
   Hint: Κρατάς τις συχνότητες των γραμμάτων του "abc". Κάθε φορά που το παράθυρο προχωράει 1 θέση, ενημερώνεις τις συχνότητες του παραθύρου (μπαίνει 1, βγαίνει 1) και συγκρίνεις.
   ```

## 10. Map Fast Lookup
1. **Λεξικό Μετάφρασης:** Φτιάξτε ένα σύστημα που παίρνει μια πρόταση και μεταφράζει τις λέξεις στα ελληνικά (όσες γνωρίζει). Η βάση (το λεξικό) θα είναι ένα Map, όπου το κάθε αγγλικό κλειδί θα φέρνει ακαριαία την ελληνική λέξη.
   ```
   Hint: Αν το dictionary["apple"] υπάρχει, τυπώνεις το value ("μήλο"). Αλλιώς τυπώνεις την αρχική λέξη. Κανένα for loop για αναζήτηση!
   ```
2. **IP Banlist:** Ένας server δέχεται χιλιάδες αιτήματα το δευτερόλεπτο (από IPs). Έχετε μια λίστα με 1.000.000 απαγορευμένες IPs. Πρέπει να ελέγχετε αν ο επισκέπτης είναι στη λίστα. Πρέπει να γίνει με map, για να μην καταρρεύσει ο server από την αναζήτηση!
   ```
   Hint: if bannedIPs[request.IP] { drop() }. Η ταχύτητα είναι κρίσιμη, το map απαντάει ακαριαία.
   ```
3. **Τοπικός Κατάλογος Προϊόντων (Cache):** Αντί να στέλνετε ερώτημα (αργό!) σε μια βάση δεδομένων για να μάθετε πόσο κάνει το Barcode `123456`, ελέγξτε πρώτα αν η τιμή υπάρχει ήδη στο τοπικό σας Map στη μνήμη RAM (cache).
   ```
   Hint: if val, ok := cache[barcode]; ok { return val }. Αλλιώς, ρώτα τη βάση και γράψε το αποτέλεσμα στο map για μελλοντική χρήση.
   ```
4. **Who is Missing?** Έχετε ένα array με τα IDs 99 μαθητών που προσήλθαν στις εξετάσεις. Γνωρίζετε ότι συνολικά οι μαθητές είναι 100 (IDs 1 έως 100). Πώς θα βρείτε (γρήγορα) ποιος απουσιάζει, χρησιμοποιώντας ένα Map αντί να ψάχνετε το array με nested loops;
   ```
   Hint: Βάλε όσους ήρθαν στο map: map[id] = true. Μετά, τρέξε ένα for από i=1 έως 100: αν !map[i], τύπωσε "Απουσιάζει ο i".
   ```
5. **Intersection of Arrays:** Έχετε δύο τεράστια arrays (εκατομμύρια αριθμούς το καθένα) και θέλετε να βρείτε ποιοι αριθμοί υπάρχουν και στα δύο. Ένα loop μέσα στο άλλο (nested) θα πάρει ώρες! Το Map θα το λύσει σε κλάσματα.
   ```
   Hint: Βάλε όλα τα στοιχεία του 1ου array σε ένα map. Στη συνέχεια, κάνε ένα loop στο 2ο array και απλά ρώτα το map αν το στοιχείο υπάρχει.
   ```

## 11. Target Lookup (Complement Map)
1. **Βρες την Έκπτωση:** Έχετε ένα εκπτωτικό κουπόνι ακριβώς 50€. Πηγαίνετε στο μαγαζί και θέλετε να βρείτε ποια 2 προϊόντα βγάζουν ΑΚΡΙΒΩΣ άθροισμα 50€, για να μην χάσετε ούτε λεπτό. Βρείτε τα χρησιμοποιώντας τη λογική του Map Complement.
   ```
   Hint: complement = 50 - price[i]. Αν το complement υπάρχει ήδη στο map, βρήκες τα δύο προϊόντα! Αλλιώς αποθηκεύεις το τρέχον.
   ```
2. **Three Sum (χρήση Two Sum):** Σας δίνεται ένα array με αριθμούς (θετικούς και αρνητικούς). Θέλετε να βρείτε 3 αριθμούς που να αθροίζουν στο 0. Η λύση κρύβεται στο να "κλειδώσετε" τον ένα, και να ψάξετε τους άλλους δύο με το Target Lookup!
   ```
   Hint: Για κάθε αριθμό arr[i], θέλεις 2 άλλους που το άθροισμά τους να κάνει -arr[i]. Χρησιμοποιείς το Map Pattern για τον στόχο -arr[i].
   ```
3. **Subarray Sum Equals K (Advanced):** Αντί να ψάχνετε δύο μεμονωμένους αριθμούς, θέλετε να βρείτε πόσα συνεχόμενα κομμάτια (subarrays) αθροίζουν σε K. Θα αποθηκεύετε στο Map τα αθροίσματα που συναντάτε (running sums).
   ```
   Hint: Αν το runningSum - K υπάρχει ήδη στο map, σημαίνει ότι υπάρχει ένα ενδιάμεσο subarray που το άθροισμά του είναι ακριβώς K!
   ```
4. **Ζευγάρι με Απόλυτη Διαφορά K:** Ψάχνετε να βρείτε δύο αριθμούς `A` και `B` σε ένα array, που η απόλυτη διαφορά τους να είναι ακριβώς K (`|A - B| = K`). Ουσιαστικά ψάχνετε να βρείτε αν ο αριθμός + K, ή ο αριθμός - K υπάρχει.
   ```
   Hint: Για κάθε αριθμό num, ρωτάς το map: Υπάρχει ο (num + K); Ή μήπως υπάρχει ο (num - K);
   ```
5. **Συμπληρωματικά Χρώματα:** Έχετε μια λίστα με χρώματα (π.χ. σε μορφή κωδικών). Θέλετε να βρείτε αν υπάρχει μέσα στη λίστα κάποιο ζευγάρι χρωμάτων που το ένα να είναι ακριβώς το αντίθετο/αρνητικό του άλλου (συμπληρωματικό).
   ```
   Hint: Για κάθε χρώμα υπολογίζεις το complement του. Αν το βρεις στο map, έχεις το τέλειο ζευγάρι. Αν όχι, βάζεις το χρώμα στο map.
   ```

## 12. Frequency Counting
1. **Μέτρηση Ψήφων (Ballot Box):** Ένα array περιέχει τα ονόματα των υποψηφίων όπως βγήκαν από την κάλπη (`["Γιώργος", "Αννα", "Γιώργος", ...]`). Μετρήστε τις ψήφους του καθενός και βρείτε ποιος κέρδισε!
   ```
   Hint: Στο loop κάνεις: votes[name]++. Όταν τελειώσει το loop, βρίσκεις ποιο κλειδί του map έχει το μεγαλύτερο value.
   ```
2. **Ιστόγραμμα Βαθμολογιών:** Σε μια τάξη οι βαθμοί κυμαίνονται από 1 έως 10. Φτιάξτε ένα ιστόγραμμα που να δείχνει πόσοι μαθητές πήραν 10, πόσοι 9, πόσοι 8 κλπ.
   ```
   Hint: Φτιάχνεις Map με keys από 1-10. Διαβάζεις το array βαθμών και κάνεις histogram[grade]++. Μετά τυπώνεις όλο το map.
   ```
3. **Duplicate IP Detection:** Θέλετε να εντοπίσετε πιθανές επιθέσεις DDoS. Σε ένα τεράστιο αρχείο καταγραφής, βρείτε ποιες IP διευθύνσεις έκαναν πάνω από 100 αιτήματα, μετρώντας τες με ένα Map.
   ```
   Hint: Διαβάζεις τις IPs και μετράς εμφανίσεις στο Map. Μετά, τρέχεις ένα loop πάνω στο Map και τυπώνεις όσες έχουν value > 100.
   ```
4. **Βρες τον Μοναχικό Αριθμό:** Σε ένα array (π.χ. `[4, 2, 1, 2, 4]`), όλοι οι αριθμοί εμφανίζονται ΑΚΡΙΒΩΣ 2 φορές, εκτός από έναν που εμφανίζεται μόνο 1 φορά. Χρησιμοποιήστε συχνότητες για να τον εντοπίσετε.
   ```
   Hint: Μετράς τις εμφανίσεις του κάθε αριθμού. Στο τέλος, ψάχνεις στο map να βρεις ποιος αριθμός (κλειδί) έχει value == 1.
   ```
5. **Group Anagrams:** Σας δίνονται λέξεις (`"act", "cat", "dog"`). Ομαδοποιήστε τες ώστε οι αναγραμματισμοί να μπουν μαζί (το act και το cat είναι στην ίδια ομάδα). Αν μετρήσετε τα γράμματά τους, θα βγάλουν την ίδια συχνότητα!
   ```
   Hint: Ταξινομείς τα γράμματα κάθε λέξης (έτσι το "cat" και το "act" γίνονται και τα δύο "act"). Χρησιμοποιείς αυτή τη λέξη ως κλειδί στο map!
   ```

## 13. Two-Pass Algorithm
1. **Κανονικοποίηση (Normalization):** Στην τεχνητή νοημοσύνη συχνά πρέπει τα δεδομένα να είναι μεταξύ 0 και 1. Σε ένα array, βρείτε το ελάχιστο και το μέγιστο (1ο πέρασμα) και μετά αλλάξτε την κάθε τιμή με τη φόρμουλα `(val - min) / (max - min)` (2ο πέρασμα).
   ```
   Hint: 1ο πέρασμα: Min/Max tracking. 2ο πέρασμα: Αλλάζεις τα δεδομένα του array με μαθηματικά, χρησιμοποιώντας το min και το max που βρήκες.
   ```
2. **Move Zeroes to End:** Σας δίνεται το array `[0, 1, 0, 3, 12]`. Πρέπει να μεταφέρετε όλα τα μηδενικά στο τέλος, αλλά οι υπόλοιποι αριθμοί (`1, 3, 12`) πρέπει να κρατήσουν τη σειρά τους. (Απαγορεύεται η χρήση extra array).
   ```
   Hint: 1ο πέρασμα: Αντέγραψε τους μη-μηδενικούς στην αρχή του ίδιου array, μετρώντας που βρίσκεσαι (index). 2ο πέρασμα: Γέμισε τις υπόλοιπες θέσεις με 0.
   ```
3. **Φιλτράρισμα Αδύναμων Σημάτων:** Θέλουμε να διαγράψουμε τις μετρήσεις ενός αισθητήρα που θεωρούνται "θόρυβος". Βρείτε τον μέσο όρο (1ο πέρασμα) και στη συνέχεια κρατήστε μόνο τα στοιχεία που είναι πάνω από το μισό του μέσου όρου (2ο πέρασμα).
   ```
   Hint: 1ο πέρασμα: Υπολογίζεις τον Average. 2ο πέρασμα: Φτιάχνεις νέο array βάζοντας μόνο τα στοιχεία που ικανοποιούν τη συνθήκη `val > avg*0.5`.
   ```
4. **Find All Numbers Disappeared:** Σε ένα array μεγέθους N, υπάρχουν νούμερα από το 1 έως το N, αλλά κάποια λείπουν και κάποια διπλοεμφανίζονται. Βρείτε ποια λείπουν, κάνοντας χρήση του ίδιου του array ως "μνήμη".
   ```
   Hint: 1ο πέρασμα: Μαρκάρεις τη θέση (index) που αντιστοιχεί στον αριθμό κάνοντάς τη αρνητική. 2ο πέρασμα: Αν μια θέση παρέμεινε θετική, ο αριθμός αυτός έλειπε!
   ```
5. **Το Σπίτι με την Καλύτερη Θέα:** Μια σειρά από σπίτια βλέπουν προς τη θάλασσα (δεξιά). Αν ένα σπίτι έχει ένα ψηλότερο κτίριο στα δεξιά του, χάνει τη θέα. Βρείτε ποια σπίτια βλέπουν τελικά θάλασσα.
   ```
   Hint: 1ο πέρασμα (ανάποδα, από δεξιά προς τα αριστερά): Φτιάχνεις array που λέει ποιο είναι το ψηλότερο κτίριο στα δεξιά. 2ο πέρασμα (κανονικά): Συγκρίνεις το ύψος του σπιτιού.
   ```

## 14. Custom Iteration
1. **Run-Length Decoding:** Σας δίνουν συμπιεσμένα δεδομένα της μορφής `"3A2B"`. Πρέπει να τα "αποσυμπιέσετε", ώστε να τυπωθεί το "A" τρεις φορές και το "B" δύο φορές (`"AAABB"`). Διαβάστε το string ανά 2 χαρακτήρες!
   ```
   Hint: Το loop κάνει άλμα `i += 2`. Στο index `i` έχεις τον αριθμό, στο index `i+1` έχεις το γράμμα. Μετά τρέχεις ένα μικρό for loop για το print.
   ```
2. **Markdown Parser:** Φτιάχνετε έναν text editor. Όποτε στο κείμενο υπάρχει διπλό αστεράκι `**`, θέλετε να το αλλάξετε σε HTML `<b>` tag. Πρέπει να προσέξετε ώστε να αγνοήσετε το δεύτερο αστεράκι, κάνοντας έξτρα βήμα.
   ```
   Hint: Αν βρεις το πρώτο `*` και το επόμενο είναι επίσης `*`, προσθέτεις το tag σου και κάνεις `i++` για να πηδήξεις το δεύτερο αστεράκι.
   ```
3. **Αφαίρεση Υποσυμβολοσειράς:** Ένα πρόγραμμα λογοκρισίας (censor) βρίσκει τη λέξη `"apple"` και θέλει να την αφαιρέσει τελείως από το κείμενο, χωρίς να διαβάσει τα γράμματά της. Το i πρέπει να "πηδήξει" ολόκληρη τη λέξη.
   ```
   Hint: Αν βρεις match ότι ξεκινάει η λέξη "apple", κάνεις `i += 5` (το μήκος της). Αλλιώς, τυπώνεις το τρέχον γράμμα και κάνεις κανονικά `i++`.
   ```
4. **Διάβασμα Matrix 2x2:** Σε επεξεργασία εικόνας, αντί να κοιτάτε ένα pixel τη φορά, θέλετε να αναλύετε τετράγωνα (blocks) 2x2. Πώς θα διαβάσετε έναν 2D πίνακα (εικόνα) χωρίς να ελέγξετε 2 φορές τα ίδια pixels;
   ```
   Hint: Τα δύο nested loops σου δεν κάνουν i++! Το εξωτερικό κάνει `y += 2` (άλμα 2 γραμμές) και το εσωτερικό κάνει `x += 2` (άλμα 2 στήλες).
   ```
5. **Jumping Game:** Κάθε κελί ενός array (π.χ. `[2, 3, 1, 1, 4]`) σας λέει το μέγιστο άλμα που μπορείτε να κάνετε. Πρέπει να βρείτε αν μπορείτε να φτάσετε στο τέλος. Εδώ, εσείς αποφασίζετε το μέγεθος του βήματος κάθε φορά.
   ```
   Hint: Δεν κάνεις ++. Παίρνεις την τιμή arr[i] και την προσθέτεις στο index (π.χ. `i = i + arr[i]`). Ελέγχεις αν το νέο i βγαίνει εκτός ορίων.
   ```

## 15. Digit Extraction
1. **Harshad Number:** Ένας αριθμός λέγεται Harshad αν διαιρείται τέλεια από το άθροισμα των ψηφίων του (π.χ. το 18 έχει ψηφία 1+8=9, και το 18 διαιρείται με το 9). Ελέγξτε αν ισχύει.
   ```
   Hint: Κάνεις extract τα ψηφία (με %10 και /10) και τα αθροίζεις σε μια temp μεταβλητή. Στο τέλος, ελέγχεις `originalNum % sum == 0`.
   ```
2. **Happy Number:** Παίρνετε έναν αριθμό, υψώνετε τα ψηφία του στο τετράγωνο και τα προσθέτετε. Το αποτέλεσμα το κάνετε ξανά το ίδιο, μέχρι να φτάσετε στο 1 (άρα είναι happy) ή μέχρι να κολλήσετε σε loop (άρα unhappy).
   ```
   Hint: Παίρνεις τα ψηφία, υπολογίζεις `(num % 10)^2` και τα προσθέτεις. Το βάζεις σε ένα while loop που ελέγχει αν φτάσεις στο 1.
   ```
3. **Δυαδικό σε Δεκαδικό (Binary to Decimal):** Έχετε τον αριθμό `1011`. Εξάγετε τα ψηφία του από δεξιά προς τα αριστερά, και μετατρέψτε τα σε δεκαδικό (0, 1, 2, 4, 8, 16...).
   ```
   Hint: Το `num % 10` σου δίνει το δυαδικό ψηφίο (0 ή 1). Το πολλαπλασιάζεις με το τρέχον `power_of_two` (ξεκινάει από 1, διπλασιάζεται σε κάθε βήμα).
   ```
4. **Palindromic Number:** Πώς θα ελέγξετε αν το `12321` είναι παλίνδρομο, χωρίς όμως να το μετατρέψετε σε string; Πρέπει να φτιάξετε (μαθηματικά) τον αντίστροφο αριθμό εξάγοντας ένα-ένα τα ψηφία.
   ```
   Hint: `reversed = reversed * 10 + (num % 10)`. Με αυτόν τον τύπο χτίζεις τον ανάποδο αριθμό, ένα ψηφίο τη φορά (σπρώχνοντας τα προηγούμενα αριστερά).
   ```
5. **Product of Digits:** Υπολογίστε το γινόμενο όλων των ψηφίων ενός αριθμού. Προσοχή: αν ο αριθμός περιέχει το ψηφίο '0' (π.χ. `105`), τότε το γινόμενο γίνεται μηδέν! Χρησιμοποιήστε Early Exit.
   ```
   Hint: `prod = 1`. Στο loop, παίρνεις `dig = num % 10`. Αν `dig == 0` κάνεις `return 0` αμέσως. Αλλιώς `prod *= dig` και `num /= 10`.
   ```

## 16. Modulo Cycles
1. **Κυκλικός Κατάλογος (Round Robin):** Έχετε έναν Load Balancer που πρέπει να μοιράσει ισόποσα εκατοντάδες requests σε ακριβώς 5 Servers (με αριθμούς 0 έως 4). Όταν φτάσει στον 4, το επόμενο request πρέπει να πάει πάλι στον 0.
   ```
   Hint: Δεν χρειάζεσαι if statements. Ο server που θα αναλάβει το request `N` είναι ο: `serverIndex = N % 5`.
   ```
2. **K-th Μετατόπιση Αλφαβήτου (Caesar Cipher):** Ένα κλασικό σύστημα κρυπτογράφησης μετακινεί κάθε γράμμα κατά K θέσεις στο αλφάβητο (0-25). Αν έχετε το 'Y' (θέση 24) και κάνετε +5, η απάντηση δεν είναι 29 (εκτός αλφαβήτου), αλλά το 'D' (3).
   ```
   Hint: `(charIndex + 5) % 26`. Η λειτουργία modulo εξασφαλίζει ότι οποιοσδήποτε αριθμός βγει εκτός του 25, θα κάνει κύκλο πίσω στην αρχή.
   ```
3. **Ticking Clock (60 Seconds):** Φτιάχνετε ένα χρονόμετρο. Κάθε φορά που περνάει 1 δευτερόλεπτο, τα δευτερόλεπτα αυξάνονται. Όταν φτάσουν στο 60, πρέπει να μηδενιστούν.
   ```
   Hint: `sec = (sec + 1) % 60`. Όταν η πράξη δώσει 0 (δηλαδή έφτασε το 60), ξέρεις ότι πρέπει να αυξήσεις τα λεπτά σου!
   ```
4. **Αλλαγή Κατεύθυνσης 2D (Compass):** Σε ένα grid, ο παίκτης κοιτάει Βορρά(0), Ανατολή(1), Νότο(2), ή Δύση(3). Αν του πείτε να στρίψει δεξιά (+1), τι γίνεται όταν κοιτάει Δύση (3) και στρίψει; Πρέπει να κοιτάξει Βορρά (0).
   ```
   Hint: Αν η κατεύθυνση είναι `dir` και στρίψει δεξιά, η νέα κατεύθυνση είναι `(dir + 1) % 4`. (Το modulo 4 αποτρέπει το να πάει στο 4).
   ```
5. **Εύρεση Μήνα:** Οι μήνες είναι 12, αλλά ξεκινούν από το 1 και όχι από το 0. Αν βρισκόμαστε στον Οκτώβριο (10) και θέλουμε να βρούμε τι μήνας θα είναι σε 15 μήνες από τώρα, πώς θα το υπολογίσουμε χωρίς να μετράμε με το δάχτυλο;
   ```
   Hint: Επειδή είναι 1-based (όχι 0-based), αφαιρούμε 1 πριν το modulo, και το ξαναπροσθέτουμε μετά: `(10 + 15 - 1) % 12 + 1`.
   ```

## 17. Stack (LIFO)
1. **Διαδρομή Φακέλου (Path Simplification):** Σε συστήματα Linux, το path `/a/./b/../../c/` έχει κρυφά νοήματα. Το `.` σημαίνει "μείνε εδώ", το `..` σημαίνει "γύρνα πίσω". Χρησιμοποιήστε μια στοίβα για να βρείτε το πραγματικό path (που είναι το `/c`).
   ```
   Hint: Αν διαβάσεις όνομα φακέλου, κάνεις Push. Αν είναι `..`, κάνεις Pop (διαγράφεις τον τελευταίο φάκελο που μπήκες). Στο τέλος ό,τι έμεινε είναι το σωστό.
   ```
2. **HTML Linter:** Ένας κειμενογράφος θέλει να ελέγξει αν κλείσατε σωστά όλα τα tags σας (π.χ. `<div><p>Hello</p></div>`). Το κλείσιμο πρέπει πάντα να ταιριάζει με το πιο *πρόσφατο* άνοιγμα.
   ```
   Hint: Στο <div> κάνεις Push. Στο </div> κάνεις Pop και ελέγχεις αν το tag που βγήκε ήταν όντως <div>. Στο τέλος το stack πρέπει να είναι εντελώς άδειο.
   ```
3. **Ιστορικό Browser (Back Button):** Πώς λειτουργεί το κουμπί Πίσω στον Browser; Όταν κάνετε κλικ σε ένα link, η τρέχουσα σελίδα μπαίνει στην "κορυφή" μιας στοίβας.
   ```
   Hint: Στο Click -> Push(URL). Όταν πατήσεις Go Back, το σύστημα κάνει Pop() και σε πάει κατευθείαν στην τελευταία σελίδα.
   ```
4. **Επόμενος Μεγαλύτερος Αριθμός:** Σας δίνεται ένα array `[2, 1, 5, 3]`. Για κάθε αριθμό, βρείτε ποιος είναι ο *πρώτος* μεγαλύτερος αριθμός στα δεξιά του (για το 2 είναι το 5, για το 1 είναι το 5, κ.ο.κ).
   ```
   Hint: Το Stack κρατάει στοιχεία που "περιμένουν" υπομονετικά να βρουν έναν μεγαλύτερο. Μόλις έρθει ένας μεγάλος αριθμός, κάνει Pop όλους τους μικρότερους!
   ```
5. **Αξιολόγηση Πράξεων με Προτεραιότητα:** Φτιάχνετε έναν υπολογιστή τσέπης που διαβάζει τα σύμβολα ανάποδα (Reverse Polish Notation), π.χ. `3 4 + 2 *`. Δεν χρειάζεται παρενθέσεις. Πώς θα το υπολογίσει η μηχανή;
   ```
   Hint: Στους αριθμούς κάνεις Push. Μόλις δεις πράξη (π.χ. +), κάνεις Pop 2 αριθμούς, τους προσθέτεις, και κάνεις Push το αποτέλεσμα πίσω στη στοίβα!
   ```

## 18. Greedy Algorithms
1. **Συμπίεση Αρχείων (Huffman Coding):** Πώς φτιάχνονται τα zip αρχεία; Ο αλγόριθμος παίρνει τα γράμματα και "άπληστα" (greedy) ενώνει πρώτα τα 2 γράμματα που εμφανίζονται πιο σπάνια (έχουν τη μικρότερη συχνότητα).
   ```
   Hint: Επιλέγεις τα 2 στοιχεία με τις μικρότερες τιμές, τα αθροίζεις σε ένα νέο στοιχείο, και τα ξαναβάζεις στη λίστα. Επαναλαμβάνεις.
   ```
2. **Ελαχιστοποίηση Καθυστερήσεων (Job Sequencing):** Είστε freelancer και έχετε πολλές δουλειές με deadlines και διαφορετικά κέρδη. Θέλετε να βγάλετε τα περισσότερα χρήματα. Η "άπληστη" λογική λέει ότι δίνετε προτεραιότητα στη δουλειά που πληρώνει περισσότερο!
   ```
   Hint: Ταξινομείς βάσει κέρδους (από το μεγαλύτερο στο μικρότερο). Παίρνεις την πιο κερδοφόρα και την τοποθετείς ΟΣΟ πιο κοντά στο deadline της γίνεται.
   ```
3. **Μοίρασμα Cookies στα Παιδιά:** Κάθε παιδί θέλει ένα μπισκότο συγκεκριμένου μεγέθους. Έχετε μπισκότα διαφόρων μεγεθών. Πώς θα τα μοιράσετε ώστε να χορτάσουν όσο το δυνατόν περισσότερα παιδιά, χωρίς να σπαταλήσετε τα μεγάλα μπισκότα;
   ```
   Hint: Ταξινομείς τα παιδιά και τα μπισκότα. Δίνεις πάντα στο πιο "απαιτητικό" (αλλά εφικτό) παιδί, το μικρότερο μπισκότο που του κάνει.
   ```
4. **Minimum Spanning Tree (Δίκτυα):** Πρέπει να ενώσετε 100 πόλεις με δρόμους, ξοδεύοντας τα λιγότερα χρήματα. Υπάρχουν χιλιάδες πιθανές συνδέσεις. Η άπληστη λογική λέει: "Φτιάξε πρώτα τον πιο φθηνό δρόμο!"
   ```
   Hint: Ταξινομείς τους δρόμους βάσει κόστους. Άπληστα επιλέγεις ΠΑΝΤΑ τον πιο φθηνό, αρκεί αυτή η προσθήκη να μην δημιουργεί "κύκλο" με τις ήδη ενωμένες πόλεις.
   ```
5. **Διάσωση με Βάρκες:** Σε ένα πλοίο που βυθίζεται, κάθε βάρκα χωράει το πολύ 2 άτομα και έχει συγκεκριμένο όριο βάρους. Πώς θα διασώσετε τους πάντες με τις λιγότερες δυνατές βάρκες; Πρέπει να γίνουν οι σωστοί συνδυασμοί βαρών!
   ```
   Hint: Ταξινομείς τα άτομα ανά βάρος. Προσπαθείς πάντα να ενώσεις τον πιο ΒΑΡΥ μαζί με τον πιο ΕΛΑΦΡΥ. Αν το άθροισμα δεν χωράει, ο βαρύς φεύγει ολομόναχος στη βάρκα.
   ```
